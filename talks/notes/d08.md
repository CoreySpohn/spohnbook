# The physical experiment, the acquired record and the assumed model

## Assets

| Asset | File | Status on the graphic |
|---|---|---|
| Still | `talks/stills/d08-experiment-record-model.png` | schematic; simulated pixels; proposed boundary |
| Still | `talks/stills/d08-fit-forecast-timeline.png` | simulated; the tutorial's fit |
| Relay still 1 | `talks/stills/d08-information-boundary.png` | schematic; proposed boundary |
| Relay still 2 | `talks/stills/d08-record-reporting-law.png` | schematic; simulated pixels |
| Relay still 3 | `talks/stills/d08-campaign-timeline.png` | schematic; proposed convention; not to scale |
| Documentation page | `docs/conventions/inference-records.md`, section `records-reporting-law` | |
| Documentation page | `docs/examples/fit-and-forecast.md`, before the orbit setup | |

## Lesson

A simulated world, an acquired record and an inference model are three different things,
and only the record, never the truth, enters inference and observing policy.

## What to say

1. Left is the simulated world: a star, a planet and the truth record that generated
   them. Truth reaches the detector only as simulated light and as the noise draw, and
   it reaches evaluation directly, along the bottom, for scoring.
2. The middle is what is actually measured. The configured visit is a command that
   points the telescope; the detector returns raw pixels. Visit 1 shows the planet, and
   the sum of the dotted pixels, 84.8 electrons, exceeds the 30 electron threshold, so
   the record keeps D = 1 and the flux. Visit 2 falls below the threshold, so under this
   reporting rule its record keeps only the event D = 0. A successful nondetection is
   data. On each card the threshold L and the noise sigma are fixed settings of the
   experiment, drawn apart from the measured fields.
3. The dashed card is a different experiment that would replace the visit 2 record:
   forced photometry reports 17.7 electrons from the same pixels even though it is below
   the threshold. Choosing between the two is a choice of likelihood.
4. Right is the model and the policy. The inference model holds the hypotheses and is
   updated from the record; the policy is a decision rule that chooses the next visit
   from the posterior only. Evaluation sits below the divider: it alone reads the truth.
   The red crossed arrows are the boundary: no truth and no score reaches inference or
   policy.
5. Second slide: the fit-and-forecast tutorial on a time axis. Cyan points are the five
   acquired epochs; pink is the posterior of the position, and each violin is the whole
   forecast distribution at days 240, 320 and 400. The forecasts are predictions, not
   data. The period ridge makes the day 400 east forecast bimodal, so no single central
   value summarizes it. Gray is the simulated truth, shown only to compare; one of six
   truths falls outside its 90 percent interval, which is expected.

## The same lessons as a relay of three slides

The overview teaches three things at once. For a slower talk, show the three relay stills
in its place. Each carries one element from the first.

1. The boundary. Three zones, only the arrows that cross them. The only arrow into the
   inference model is the record of each visit, drawn as one card listing its epoch, event
   D sub k, offsets xi sub k and eta sub k, and covariance C sub k. Truth reaches the
   detector as light and the noise draw, and reaches evaluation; the two red crosses are
   the paths that must not exist.
2. The record, opened. The same card, now for the chapter's scalar flux example. Both
   visits go on one F axis with the threshold L at 30 electrons. Visit 1 lands at 84.8
   electrons, above L, and gives the same record under either law. Visit 2 lands at 17.7
   electrons, below L: the reduction computes it, but reporting only on detection keeps
   just D = 0, so its marker is hollow; only forced photometry, a different experiment,
   releases the flux. The carried card now lists F sub k and sigma squared in place of
   the offsets and C sub k. The bars are plus or minus
   sigma, 6.0 electrons, at the scale of the axis.
3. The loop on a time axis. The policy box from the first slide heads the top lane. Read
   left to right: decide, admit, expose, complete, release, infer, and only then decide
   again; the next decision may use only records already released. The dotted line marks
   that decision. Every allowed arrow points forward in time. A later recalibration of visit k
   may inform later updates, but the red cross says it can never reach back to the
   decision already made.

## Assumptions

- The pixels are simulated: a known 20 electron background per pixel, a
  pixel-integrated Gaussian planet image and independent Gaussian noise of 2 electrons
  per pixel. The reduction subtracts the background and sums the 9 pixels within 1.5
  pixels of the fixed target position (the 3 by 3 block), so the flux has a standard
  deviation of 6.0 electrons before any selection.
- The detection rule is the chapter's scalar example: D = 1 precisely when F > L for a
  fixed, known threshold L.
- The information boundary is a proposed convention. The picture does not assert that
  the current tutorial or campaign software enforces every separation it shows.
- The forecast figure reruns the tutorial's own simulation and fit, with the same orbit,
  epochs, random key and samplers. It uses the fitting library's current conventions and
  is an example implementation, not a validation.

## Source locators

- `records-reporting-law`, `records-identities` and `records-campaign-causality` in
  Measurements, probability, and records. The time order on the third relay still is the
  one `records-campaign-causality` states: admission, completion, release, inference,
  then policy.
- Efron and Hastie (2016), Sec. 9.6: censoring versus truncation, for the difference
  between report-only-on-detection and forced photometry.
- Loredo (2004), the survey-likelihood footnote in its trans-Neptunian section: the
  selection event and the retained measurement in one probability model.

## Numbers on the graphic

| Number | Value | Source (script and parameter, or reference) |
|---|---|---|
| Threshold L | 30 electrons | `tools/explainers/d08_experiment_record_model.py`, `THRESHOLD_E` |
| Visit 1 flux F | 84.8 electrons | aperture sum of the drawn visit 1 pixels (`visit_images`, `reduce`) |
| Forced photometry F | 17.7 electrons | aperture sum of the drawn visit 2 pixels |
| Flux standard deviation | 6.0 electrons | `PIXEL_NOISE_E` times the square root of 9 |
| F axis of relay still 2 | 0 to 100 electrons | `F_AXIS_E`; markers, threshold and error bars drawn on it |
| Astrometric noise | 5 mas per axis | `SIGMA_MAS`, equal to the tutorial's `sigma_mas` |
| Acquired epochs | -140, -70, 0, +70, +140 days | `T_OBS_D`, equal to the tutorial's `t_obs` |
| Forecast epochs | +240, +320, +400 days | `T_FUTURE_D`, equal to the tutorial's `t_future` |
| Reference day | JD 2461160 | `T_REF_JD`, equal to the tutorial's `t_ref_jd` |

All numbers are checked by `tests/test_explainer_d08.py`.
